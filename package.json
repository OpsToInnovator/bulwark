{
  "name": "bulwark",
  "version": "1.0.0",
  "description": "Drop-in LLM API proxy with hard spend caps, bedtime mode, and caching",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.95.0"
  },
  "dependencies": {
    "itty-router": "^5.0.18"
  }
}
